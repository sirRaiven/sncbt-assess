# Merge only these values into your CURRENT supabase/config.toml.
# Do NOT replace the whole config file with this snippet.

[auth]
minimum_password_length = 8
password_requirements = "lower_upper_letters_digits"

[auth.email]
enable_confirmations = true
secure_password_change = true
max_frequency = "1m"
